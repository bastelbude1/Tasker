# TEST_METADATA: {"description": "Basic environment variable expansion in global variables", "test_type": "positive", "expected_exit_code": 0, "expected_success": true}

# Test basic environment variable expansion
MY_USER=$USER
MY_HOME=$HOME
MY_HOSTNAME=$HOSTNAME

task=0
hostname=localhost
command=echo
arguments=User: @MY_USER@
exec=local

task=1
hostname=localhost
command=echo
arguments=Home: @MY_HOME@
exec=local

task=2
hostname=localhost
command=test
arguments=@MY_USER@ = baste
exec=local
condition=@MY_USER@=baste
