# TEST_METADATA: {"description": "Conditional Block test - empty branches and edge cases", "test_type": "positive", "expected_exit_code": 0, "expected_success": true, "expected_execution_path": [0, 3], "expected_skipped_tasks": []}

# Test conditional block with empty branches (no tasks specified)
task=0
hostname=localhost
command=echo
arguments=TEST
exec=local

# Conditional with empty true branch (edge case)
task=1
type=conditional
condition=@0_stdout@=NOMATCH
if_true_tasks=
if_false_tasks=

# Another conditional with only false branch defined
task=2
type=conditional
condition=@0_stdout@=TEST
if_true_tasks=
if_false_tasks=99

# Task 99 should be skipped since condition is true
task=99
hostname=localhost
command=echo
arguments=This should be skipped - false branch not taken
exec=local

# Final task to confirm workflow continues
task=3
hostname=localhost
command=echo
arguments=Edge cases handled correctly
exec=local