# TEST_METADATA: {"description": "Test alert script execution on task failure", "test_type": "negative", "expected_exit_code": 1, "expected_success": false}

# Test that --alert-on-failure is executed when a task fails
# This test requires manual execution with the alert script

task=0
hostname=localhost
command=echo
arguments=Task 0 succeeded
exec=local
success=exit_0

task=1
hostname=localhost
command=false
arguments=
exec=local
success=exit_0
