# TEST_METADATA: {"description": "Test warning when alert script is missing", "test_type": "negative", "expected_exit_code": 1, "expected_success": false}

# Test that TASKER warns when alert script doesn't exist
# Workflow should still fail with proper exit code even if alert fails

--alert-on-failure=/nonexistent/path/to/alert.sh

task=0
hostname=localhost
command=echo
arguments=Task starting
exec=local
success=exit_0

task=1
hostname=localhost
command=false
arguments=
exec=local
success=exit_0
