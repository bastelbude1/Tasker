# TEST_METADATA: {"description": "Test that validation warns about shell syntax in exec=local", "test_type": "security_negative", "expected_exit_code": 20, "expected_success": false}

# Test: Validation should warn users about shell syntax in exec=local
# This prevents user mistakes (shell syntax won't execute with exec=local anyway)
# For actual shell execution, users should use exec=shell
task=0
hostname=localhost
command=echo
arguments=test $(whoami) injection
exec=local
