# TEST_METADATA: {"description": "Instance control - force-instance flag blocked in files", "test_type": "negative", "expected_exit_code": 20, "expected_success": false}

# Test that --force-instance is BLOCKED when used as file-defined argument
# SECURITY: --force-instance must only be usable via command line to prevent
# malicious or accidental task files from bypassing instance control

# File-defined arguments
--instance-check
--force-instance

# Global variables
TEST_VAR=force_instance_test

# Tasks
task=0
hostname=localhost
exec=local
command=echo
arguments=Testing force-instance override
