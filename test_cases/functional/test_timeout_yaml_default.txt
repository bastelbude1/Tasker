# TEST_METADATA: {"description": "Test YAML platform default timeout (300s)", "test_type": "positive", "expected_exit_code": 0, "expected_success": true, "skip_host_validation": true}

# Test that tasks without explicit timeout use YAML platform default (300s)
# This is currently configured in cfg/execution_types.yaml

task=0
hostname=localhost
exec=local
command=echo
arguments=Testing YAML platform default timeout

# Task without timeout parameter - should use platform default from YAML (300s)
task=1
hostname=localhost
exec=local
command=sleep
arguments=2
# No timeout specified - uses YAML default_timeout: 300

task=2
hostname=localhost
exec=local
command=echo
arguments=YAML default timeout test completed