# TEST_METADATA: {"description": "Resume from task 0 (baseline - normal start)", "test_type": "positive", "expected_exit_code": 0, "expected_success": true, "start_from_task": 0, "expected_execution_path": [0, 1, 2], "skip_host_validation": true, "expected_warnings": 1}

# Resume Capability Test: Resume from Start (Task 0)
#
# Purpose: Baseline test - validate that --start-from=0 works identically to normal execution
#
# Test Scenario:
# 1. Use --start-from=0 (explicit task 0 start)
# 2. All tasks (0, 1, 2) should execute normally
# 3. No tasks should be skipped
#
# Expected Behavior:
# - Resume mode logs "Resume mode: Starting from Task 0"
# - All tasks execute in order: 0 -> 1 -> 2
# - Exit code: 0 (success)

task=0
hostname=localhost
command=echo
arguments=Task 0 - Start task
exec=local

task=1
hostname=localhost
command=echo
arguments=Task 1 - Middle task
exec=local

task=2
hostname=localhost
command=echo
arguments=Task 2 - Final task
exec=local
